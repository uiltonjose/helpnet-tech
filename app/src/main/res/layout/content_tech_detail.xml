<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        style="@style/CardViewItem"
        app:cardCornerRadius="4dp"
        app:cardUseCompatPadding="true">

    <LinearLayout
            style="@style/CardViewContainer"
            android:orientation="vertical">

        <TextView
                style="@style/TextTitleDetail"
                android:drawableStart="@drawable/ic_tech_verified"
                android:drawablePadding="12dp"
                android:text="@string/tech_responsible_label"/>

        <View android:layout_width="match_parent" android:layout_height="1dp"
              android:layout_marginTop="10dp"
              android:background="@color/separator_color"/>

        <TextView
                android:id="@+id/techResponsibleTextView"
                style="@style/TextDetail"
                tools:text="Técnico: Jarvis Computer"/>
    </LinearLayout>
</androidx.cardview.widget.CardView>